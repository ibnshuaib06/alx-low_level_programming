#include "main.h"
/**
 * add - a function that adds two integers and returns the result.
 *
 * @a: an integer
 * @b: another intger to be add to a
 *
 * Return: the result
 */
int add(int a, int b)
{
	int c;

	c = a + b;
	return (c);
}
